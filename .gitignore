# 隐私：config.json 含 DeepSeek API Key、余额、重置时间等本地私有数据，绝不入库
config.json

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# 打包产物（exe 由 PyInstaller 自行构建；zip 作为 Release 资产单独上传）
*.exe
*.zip
build/
dist/
*.spec

# 系统
Thumbs.db
.DS_Store
desktop.ini

# 验证预览截图（不入库）
preview/
preview_api/
